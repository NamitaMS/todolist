# todolist
A command-line task manager built in Python. Add, edit, complete, and delete tasks, filter by status (all/active/completed), assign priority levels, and clear completed tasks in bulk. Tasks are saved automatically to a local JSON file, so your list persists between sessions — no database or external dependencies required.
